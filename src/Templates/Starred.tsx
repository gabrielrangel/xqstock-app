import Typography from "@mui/material/Typography/Typography";
import { FunctionComponent } from "react";

export const Starred: FunctionComponent = () => (
  <Typography variant="h1" component="h1">
    Starred
  </Typography>
);

export default Starred;
